import React from 'react';


const NoChat = () => {

  return (
    <div className="chat" icon="pi pi-send" style={{fontSize:'larger',background:"#ddddf7",display:'flex',justifyContent:'center',alignItems:'center'}}>
      <span>choose a chat to start a conversation</span>
    </div>
  );
};

export default NoChat;
